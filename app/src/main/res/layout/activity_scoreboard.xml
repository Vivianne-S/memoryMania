<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#075D92"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- Scoreboard Title -->
    <TextView
        android:id="@+id/scoreboard_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:text="@string/score_board_title"
        android:textColor="#FE5900"
        android:textSize="44sp"
        android:textStyle="bold"
        android:layout_marginTop="20dp"/>

    <!-- Scoreboard List (Just Times) -->
    <TextView
        android:id="@+id/score_board_list"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:lineSpacingExtra="12dp"
        android:layout_gravity="center"
        android:text="@string/score_board_list"
        android:textColor="#FFFFFF"
        android:textSize="28sp" />

    <!-- Back to Menu Button -->
    <Button
        android:id="@+id/back_to_menu_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginTop="20dp"
        app:layout_constraintTop_toBottomOf="@id/score_board_list"
        android:backgroundTint="#FE5900"
        android:text="@string/back_to_menu"
        android:textColor="#FFFFFF"/>
</LinearLayout>
